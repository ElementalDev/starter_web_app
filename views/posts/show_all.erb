<!-- Show all the books -->
<% @books.each do |book| %>
  <div>
    <h3>
      <a href="/books/<%= book[:id] %>"> <%= book[:title] %> </a>
    </h3>
    <p><%= book[:author] %></p>
  </div>
<% end %>
